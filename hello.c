#include <stdio.h>
int  main()
{
     int a,b, area;
      printf("enter the value of length and breath");
      scanf("%d" "%d",a, b);
   area= a*b;
   printf("%d",area);
   getch();
   return 0;
}
